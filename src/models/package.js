export default class Package{
    constructor(weight, width, height,length, position){
        this.weight = weight;
        this.width = width;
        this.height = height;
        this.length = length;
        this.position = position;
    }
}